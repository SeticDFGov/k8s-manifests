apiVersion: apps/v1
kind: Deployment
metadata:
  name: meu-front-deployment
  labels:
    app: meu-front
spec:
  replicas: 1
  selector:
    matchLabels:
      app: meu-front
  template:
    metadata:
      labels:
        app: meu-front
    spec:
      containers:
        - name: meu-front-container
          image: sousacomz/sgdp-new-front:564e257
          imagePullPolicy: Always
          ports:
            - containerPort: 4200
